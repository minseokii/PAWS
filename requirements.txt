# Core deps for PAWS (Python 3.10 + PyTorch 2.1 + CUDA 11.8)
# PyTorch / torchvision are installed separately via the official index.
# Pin numpy < 2 because PyTorch 2.1 is built against NumPy 1.x.

numpy>=1.24,<2
opencv-python
pandas
matplotlib
graphviz
pillow
pyyaml
tqdm
requests

# Config / serialization
easydict
yacs>=0.1.8

# Build helpers (Cython / CUDA extensions)
cython
ninja

# Build tooling (pkg_resources used by torch.utils.cpp_extension)
setuptools<81

# COCO API (used by maskrcnn_benchmark hooks)
pycocotools

# Pickling / I/O
h5py
dill

# Cityscapes hooks expected by maskrcnn_benchmark imports
cityscapesscripts

# SGG embeddings + transformer modules
timm>=0.9
einops

# Logging
tensorboardX
wandb
